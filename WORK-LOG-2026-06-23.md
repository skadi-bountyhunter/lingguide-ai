# 工作日志 — 2026-06-23

## 概览

今日主任务围绕**推荐路线页地图模块的精度与可管理性**展开，共四项：

1. 景点坐标用高德 POI 搜索校准（消除手填坐标偏移）
2. 自定义 marker 锚点修复（消除缩放偏移）
3. 路线由直线改为高德步行路径规划
4. 新增「路线管理」后台（预设路线 CRUD）

```
阶段六 ████████████ 地图模块改造        100% ✅（昨日主体 + 今日精度强化）
新增    ████████████ 路线管理后台        100% ✅（今日完成）
```

---

## 一、景点坐标 POI 校准

### 1.1 问题
手填的 6 个景点经纬度与高德底图真实位置有偏移，地图标注对不齐。

### 1.2 方案
用高德 JS API 的 `PlaceSearch` 插件按景点名在无锡范围内搜首个 POI 真实坐标，覆盖手填值。复用现有 Web key（JS API key 与 Web 服务 key 通常一致），无需另开 Web 服务 key。

### 1.3 实现
- `scripts/calibrate_spots.html`：校准页。点「开始校准」逐个 POI 搜索，表格展示新旧坐标对比，点「写回后端」调 `PUT /api/spots/{id}` 更新
- `scripts/inspect-calibrate-spots.js`：CDP 自动化跑校准流程并截图
- `scripts/inspect-wuyin-search.js`：备选关键词搜索（"五印坛城"首次无结果时用"灵山胜境五印坛城"命中）

### 1.4 校准结果

| 景点 | 旧坐标 | 新坐标（高德 POI） |
|------|--------|------|
| 灵山大佛 | 120.1032, 31.4247 | 120.096477, 31.430194 |
| 梵宫 | 120.0995, 31.4228 | 120.102413, 31.428263 |
| 九龙灌浴 | 120.1003, 31.4235 | 120.099984, 31.424601 |
| 五印坛城 | 120.0972, 31.4215 | 120.103054, 31.424676 |
| 降魔浮雕 | 120.1038, 31.4252 | 120.099569, 31.425559 |
| 菩提大道 | 120.0985, 31.4220 | 120.101143, 31.423182 |

6 个全部校准写回数据库。

---

## 二、地图标记锚点修复

### 2.1 问题
缩放地图时自定义 pin 与实际位置有偏移。

### 2.2 根因
原水滴形 pin 用 `border-radius: 50% 50% 50% 0` + `rotate(-45deg)` 实现，旋转后尖端位置随 transform 浮动，不固定在 div 底部中心；同时 marker 同时设了 `anchor` 和 `offset`，两者冲突。

### 2.3 修复 `frontend-visitor/src/components/ScenicMap.vue`
- pin 改为圆形 badge + `::after`/`::before` 三角做尖端，尖端顶点固定在 wrap 的 (18, 44) 像素位置
- marker 只用 `offset: {x:-18, y:-44}`，去掉 `anchor`
- CSS：`.map-pin-wrap` 36×44，`.map-pin-badge` 36×36 圆形，`.map-pin-wrap::after` 白三角 + `::before` 绿三角组成尖端，`.map-pin-num` 序号角标

无论缩放多少倍，锚点始终是 div 内固定像素点，不再偏移。

---

## 三、步行路线规划

### 3.1 问题
原「路线」功能只是把 6 个景点按顺序用虚线直线连起来，两点间直线穿过，不绕路。

### 3.2 实现 `ScenicMap.vue`
- 加载 `AMap.Walking` 插件
- `showRoute` 改为分段步行规划：按景点游览顺序，对相邻两点调 `walking.search`，取每段步行坐标序列合并成一条折线
- 加 8s 超时兜底：单段规划超时或失败则退化为直线，不卡死
- 工具栏加 `routeStatus` 状态提示（规划中 / 已生成 N 个节点 / 失败）

### 3.3 踩坑
- 初版用 `policy: AMap.WalkingPolicy.LEAST_WALK`，但 `AMap.WalkingPolicy` 未加载导致构造异常被外层 catch 吞掉，回调永不触发，一直卡在「规划中」。去掉 policy 后正常。
- 直接测试 `new AMap.Walking({}).search` 回调 status=complete 正常，定位到是配置项问题。

### 3.4 结果
5 段步行路径全部规划完成并合并，共 252 个节点，路线贴合景区真实步行道路。

---

## 四、路线管理后台

### 4.1 后端
- `backend/app/models/route.py`：`Route` 模型（title/icon/duration/distance/difficulty/desc/spots[]/tags[]/tip/sort_order），JSON list 字段用 @property getter/setter（同 Spot/FAQ 模式）
- `backend/app/api/routes.py`：`/api/routes` CRUD
  - 公开：`GET /api/routes`（列表）、`GET /api/routes/{id}`
  - 管理：`POST` / `PUT /{id}` / `DELETE /{id}`（无鉴权，同 knowledge/profile/spots 一致）
  - `SEED_ROUTES`：5 条预设路线（佛韵深度游/山水揽胜游/亲子欢乐游/建筑艺术游/禅意素心游），`seed_routes()` 在 lifespan 调用
- `backend/app/models/__init__.py`：导入 Route 确保建表
- `backend/app/main.py`：注册 routes.router + lifespan 调 seed_routes

### 4.2 管理端 `frontend-admin`
- `src/views/RoutesView.vue`：表格（图标/路线名/包含景点/标签/时长/难度/排序/操作）+ 编辑对话框（基本信息/包含景点/分类标签/游览贴士分组），复用 SpotsView 的 ElMessage/ElMessageBox 模式
- `src/router.ts`：路由 `/routes`
- `src/App.vue`：侧边栏「路线管理」菜单项 + 标题映射「预设路线管理」

### 4.3 游客端 `frontend-visitor`
- `src/views/RouteView.vue`：`presetRoutes` 从硬编码数组改为 `ref` + `onMounted` 从 `/api/routes` 取数，删除原 5 条硬编码数据
- 接口返回字段（title/icon/duration/distance/difficulty/desc/spots/tags/tip）与模板一致，无需转换

### 4.4 验证
- API：5 条种子路线返回正常，CRUD 全流程（新增 id=6 → 改 duration=2h → 删除 → 剩余 5 条）通过
- 字段：每条 spots=4、tags/duration/difficulty 完整

---

## 五、改动文件清单

| 文件 | 改动 |
|------|------|
| `scripts/calibrate_spots.html` | 新增，POI 校准页 |
| `scripts/inspect-calibrate-spots.js` | 新增，CDP 自动校准 |
| `scripts/inspect-wuyin-search.js` | 新增，备选关键词搜索 |
| `frontend-visitor/src/components/ScenicMap.vue` | marker 锚点修复 + 步行路线规划 |
| `backend/app/models/route.py` | 新增，Route 模型 |
| `backend/app/api/routes.py` | 新增，routes CRUD + 种子 |
| `backend/app/models/__init__.py` | 导入 Route |
| `backend/app/main.py` | 注册 routes 路由 + seed_routes |
| `frontend-admin/src/views/RoutesView.vue` | 新增，路线管理页 |
| `frontend-admin/src/router.ts` | 加 /routes 路由 |
| `frontend-admin/src/App.vue` | 侧边栏 + 标题映射 |
| `frontend-visitor/src/views/RouteView.vue` | presetRoutes 改接口取数 |

---

## 六、风险点与解决

1. **高德 POI 搜索 key 类型**：JS API key 不能用于 Web 服务 REST（`USERKEY_PLAT_NOMATCH`），但 JS API 的 `PlaceSearch` 插件可用同一 key，故选前端方案而非后端 REST
2. **marker 锚点偏移**：`anchor` 与 `offset` 不可同用，且旋转 transform 会让锚点浮动；改用固定像素三角 + 纯 offset
3. **Walking 规划卡死**：`AMap.WalkingPolicy` 未加载时构造抛异常被吞，回调不触发；去掉 policy 配置 + 加超时兜底
4. **后端 reload 不生效**：`--reload` 对新增模块的顶层 import 改动有时不触发完整重载，且多进程残留占端口；彻底停掉后台任务后无 reload 干净重启解决
5. **bash JSON 转义**：Windows bash 下 curl 传 JSON body 引号/emoji 被转义破坏，改用 `--data-binary @file` 传文件

---

## 七、运行状态

- 后端: localhost:**8000**（routes API 已就绪，5 条种子路线）
- 游客端: localhost:3000（路线页 `/route` 地图标记已校准、步行路线已生效、预设路线取后端）
- 管理端: localhost:3001（`/routes` 路线管理已可用）
- 两端 vite proxy 均指向 :8000

---

## 八、后续可做

- 路线管理的「包含景点」目前是自由输入，可改为从 `/api/spots` 已有景点中多选
- 步行路线可加总距离/总时长汇总显示
- 地图标记点击可高亮对应路线卡片联动
