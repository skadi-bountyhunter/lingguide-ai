# 天气接口与无锡灵山锁定修复报告

## 1. 基本信息

- 日期：2026-07-29
- 项目：灵境导游（LingGuide）
- 涉及功能：游客端天气卡片、AI 对话天气查询、高德 Web 服务调用
- 安全说明：本文不记录任何 API Key、App Secret、访问令牌或密码。

## 2. 问题描述

本轮处理以下问题：

1. 游客端天气 API 显示不可用。
2. 历史上查询“灵山”时，高德地理编码曾匹配到福建同名地点。
3. 需要将景区天气卡片和默认对话语境固定为无锡灵山胜境，同时保留用户明确查询其他城市天气的能力。

## 3. 排查结论

### 3.1 天气接口失效原因

本机排查时，后端端口 `8000` 未监听，因此游客端虽然运行在 `3000`，但 `/api/weather` 无法通过 Vite 代理访问后端。

使用当前 `backend/.env` 直接调用高德天气链路后，结果为：

- `AMAP_WEB_KEY` 已成功加载；
- 高德 Web 服务 Key 当前有效；
- 查询状态为 `ready`；
- 实况天气正常；
- 返回 4 天预报；
- 查询地点为无锡灵山胜境。

因此，本轮现场的直接失效原因是**后端未启动**，不是当前高德 Key 失效。

### 3.2 配置来源分叉

此前存在两套后端配置来源：

- 本地直接启动时，`backend/app/config.py` 固定读取 `backend/.env`；
- Docker Compose 的 backend 服务读取根目录 `.env`；
- 根目录 `.env` 未配置 `AMAP_WEB_KEY`。

这会导致同一份代码在不同启动方式下表现不一致：本地直启可用，Compose 启动可能返回 `missing_api_key`。

### 3.3 福建灵山问题

现行底层代码已经有景区别名归一化和固定行政区划码逻辑，但仍存在两个边界：

- `/api/weather` 公开接受 `city`、`scope`，调用方可以覆盖景区地点；
- 默认配置可被环境变量改成其他地点，缺少强约束。

本轮进一步在服务端关闭地点覆盖，并将默认景区配置锁定为：

| 字段 | 固定值 |
|---|---|
| 景区 | 灵山胜境 |
| 城市约束 | 无锡 |
| 高德 adcode | `320211`（无锡市滨湖区） |

默认景区请求直接使用 `320211`，不再执行模糊地理编码，因此不会接触福建同名候选。

## 4. 已完成修改

### 4.1 固定无锡灵山配置

文件：`backend/app/config.py`

- 集中定义 `灵山胜境 / 无锡 / 320211`。
- 增加配置校验：如果环境变量把景区天气三元组改成其他地点，后端启动时直接报错，避免静默漂移。
- 对 `AMAP_WEB_KEY` 去除首尾空白；空串或纯空白统一视为未配置。

### 4.2 锁定景区天气 API

文件：`backend/app/api/weather.py`

- `/api/weather` 移除公开的 `city`、`scope` 参数。
- 仅保留 `locale` 参数。
- 后端始终使用 Settings 中固定的无锡灵山地点调用天气服务。
- 即使 URL 携带 `city=福建灵山&scope=福州`，这些参数也不会参与天气查询。

### 4.3 固定 adcode 快速路径

文件：`backend/app/core/tools/amap_tools.py`

- 本景区别名和景区上下文统一走固定 `320211`。
- 固定路径不调用 geocode。
- 展示地点由统一配置生成，避免多处硬编码地点名称发生漂移。
- 默认景区和外地天气继续使用不同缓存键，避免外地结果污染景区缓存。

### 4.4 改进高德错误诊断

文件：`backend/app/core/tools/amap_tools.py`

- 高德返回业务错误时，保留安全的 `infocode`。
- 记录失败阶段：`geocode`、`forecast` 或 `live`。
- 对游客仍返回通用错误文案，不暴露第三方原始响应。
- 日志不记录请求参数、完整带查询字符串 URL 或 Key。

文件：`backend/app/services/query_coordinator.py`

- 天气 trace 增加非敏感的 `provider_code` 和 `stage`，便于定位 Key 类型、权限、配额、参数或网络问题。

### 4.5 统一环境配置来源

文件：`docker-compose.yml`

- backend 的 `env_file` 从根 `.env` 改为 `./backend/.env`。
- 本地直启和 Compose 现在使用同一份后端环境配置。

文件：`.env.example`

- 增加空占位 `AMAP_WEB_KEY=`。
- 增加 `WEATHER_DEFAULT_ADCODE=320211`。
- 注明后端必须使用高德 **Web 服务 Key**，不能使用前端 JavaScript API Key。
- 未写入任何真实凭据。

### 4.6 对话地点策略

文件：`backend/app/services/query_coordinator.py`（复用现有逻辑并补充回归验证）

以下表达固定为无锡灵山：

- 灵山、灵山景区、灵山胜境、灵山大佛、无锡灵山；
- 这里、本景区、附近、本园区、当前景点等景区上下文；
- 未明确地点的天气问题，例如“今天需要带伞吗”。

以下明确地点仍尊重用户意图：

- 杭州天气；
- 上海天气；
- 明确的福建灵山天气。

系统不保存“上一次天气城市”为会话默认值。因此先问“福建灵山天气”，再问“这里天气”，第二句仍会回到无锡灵山，不继承福建地点。

## 5. 测试修改

### 5.1 更新测试

- `backend/tests/test_amap_tools.py`
  - 验证默认景区直接使用 `320211` 且不调用 geocode；
  - 验证传入 `scope=福州` 也不能覆盖本景区；
  - 验证明确外地灵山不会被错误重写；
  - 验证景区天气仅发起 forecast/live 两次请求；
  - 验证 provider 错误码和阶段可诊断且不泄露测试 Key。

- `backend/tests/test_weather_fallback.py`
  - 修正旧的“景区先 geocode、总共三次调用”断言；
  - 验证默认天气请求全部使用 `city=320211`；
  - 验证“福建灵山 → 这里”不会产生地点污染；
  - 保留杭州等明确城市查询能力。

### 5.2 新增测试

- `backend/tests/test_weather_api.py`
  - 验证 `/api/weather` 始终调用无锡灵山；
  - 验证 OpenAPI 只公开 `locale`；
  - 验证 URL 中的福建地点参数不起作用；
  - 验证错误响应不泄露 Key 或原始请求参数。

## 6. 验证结果

### 6.1 天气定向测试

```text
44 passed, 1 warning
```

警告为项目已有的 FastAPI Operation ID 重复提示，与天气修复无关。

### 6.2 真实高德接口

验收结果：

```text
health_status = healthy
weather.status = ready
weather.place = 无锡灵山胜境
weather.has_live = true
weather.forecast_count = 4
```

带恶意覆盖参数的请求：

```text
/api/weather?locale=zh-CN&city=福建灵山&scope=福州
```

仍返回：

```text
place = 无锡灵山胜境
```

OpenAPI 参数：

```text
[locale]
```

### 6.3 对话天气

实测结果：

| 输入 | 结果地点 |
|---|---|
| 灵山天气怎么样 | 无锡灵山胜境 |
| 这里天气怎么样 | 无锡灵山胜境 |
| 本景区今天需要带伞吗 | 无锡灵山胜境 |
| 杭州天气怎么样 | 浙江省杭州市 |
| 福建灵山天气怎么样 | 福建省宁德市蕉城区灵山 |
| 外地查询后再问“这里天气” | 无锡灵山胜境 |

### 6.4 浏览器页面

通过 Chrome CDP 自动检查 `http://127.0.0.1:3000/route`：

- 天气卡片显示“无锡灵山胜境”；
- 实况和预报正常显示；
- 页面未出现福建、福州或闽侯；
- 页面未显示天气服务错误；
- 前端天气请求仅包含 `locale`。

### 6.5 后端全量测试

```text
212 passed, 12 failed, 2 warnings
```

12 个失败均不在本次天气修改范围：

- 3 个 FAQ 意图测试仍按旧 `_faq_covers_named_spots()` 方法签名调用；
- 8 个 RAG/LLM 集成与响应时延测试受当前检索/外部 LLM 状态影响；
- 1 个 RAG 评估器测试期待已不存在的 `no_evidence_proxy` 字段。

这些失败未由本轮天气改动引入，后续应作为独立任务处理。由于全量测试未全绿，本报告不把后端整体状态标记为完全通过。

## 7. 用户后续复测步骤

### 7.1 启动后端

```bat
cd /d E:\ruanjianbei\backend
python -m uvicorn app.main:app --reload --port 8000 --host 0.0.0.0
```

看到以下输出即表示服务启动：

```text
Uvicorn running on http://0.0.0.0:8000
```

### 7.2 检查接口

浏览器打开：

```text
http://localhost:8000/api/health
http://localhost:8000/api/weather?locale=zh-CN
```

天气接口应满足：

- `ok=true`；
- `status=ready`；
- `place` 为无锡灵山胜境；
- `live` 或 `casts` 有数据；
- 不出现福建、福州、闽侯。

### 7.3 检查游客端

启动游客端：

```bat
cd /d E:\ruanjianbei\frontend-visitor
npm run dev
```

访问：

```text
http://localhost:3000/route
```

确认天气卡片显示无锡灵山胜境，并测试刷新与切换语言。

### 7.4 检查对话

依次询问：

1. “灵山天气怎么样”；
2. “这里天气怎么样”；
3. “本景区今天需要带伞吗”；
4. “杭州天气怎么样”；
5. “福建灵山天气怎么样”；
6. “这里天气怎么样”。

第 1、2、3、6 条必须是无锡灵山；第 4、5 条应尊重明确地点。

## 8. 当前服务状态

为自动验收临时启动的后端和 Chrome 调试进程已停止。代码和配置修改已保留，但用户复测前需要重新启动后端 `8000`。

## 9. 后续建议

1. 演示前先检查 `http://localhost:8000/api/health`，避免前端已启动但后端未启动。
2. 高德 Key 必须使用 Web 服务类型；地图能显示不代表天气 REST Key 可用。
3. Key 轮换后必须重启后端，Settings 和天气缓存不会自动读取新环境变量。
4. 不要在日志、截图、文档或测试中输出真实 Key。
5. 后端全量测试现已恢复全绿；后续可单独处理现存的两个非阻塞 warning。

## 10. 后端全量 12 项失败的后续修复与复验

### 10.1 失败归因

天气修复完成后的首次全量测试为：

```text
212 passed, 12 failed, 2 warnings
```

进一步排查确认 12 项失败分为三类：

1. **FAQ 意图测试 3 项**
   - `_faq_covers_named_spots()` 已改为显式接收 `structured_results`；
   - 三个旧测试仍按双参数签名调用，并 monkeypatch 已不再由该纯判断函数使用的检索器；
   - 生产调用已经使用新签名，不需要恢复旧兼容或隐藏 I/O。

2. **RAG API 测试 8 项**
   - `test_rag.py` 通过 `/api/chat/text` 运行完整生产链路，但没有隔离真实 DeepSeek；
   - DeepSeek 请求失败时，生产安全契约会调用 `mark_generation_failure()`，主动清空候选结果和引用，因而 6 个用例得到空 `sources`；
   - 两个不同问题都会返回同一条安全拒答，因此原“回复文本不同”断言失败；
   - 真实网络请求和重试导致性能测试超过 10 秒；
   - canonical、FTS 和 Chroma 的 36 个 active chunk 均正常，该问题不是索引损坏。

3. **RAG 评估器测试 1 项**
   - 测试仍期待 `rag-baseline-v1` 的 `no_evidence_proxy`；
   - 当前 `rag-baseline-v2` 已把生产拒答样例通过 `production_refusal_cases` 委托给 `lingguide-demo-v1`；
   - 实际拒答指标由 demo evaluator 的 `refusal_accuracy` 负责，旧字段不应恢复。

### 10.2 修改内容

#### FAQ 测试契约

文件：`backend/tests/test_faq_intent.py`

- 删除 3 个失效的 `structured_retriever.search` monkeypatch；
- 使用现有 `_spot()` helper 构造结构化结果；
- 显式调用 `_faq_covers_named_spots(query, faq, structured_results)`；
- 保留“泛化亲子 FAQ 不覆盖点名景点”“单 FAQ 不覆盖多景点”“景点自己的 FAQ 可短路”三种语义。

生产文件 `backend/app/services/query_coordinator.py` 未修改，仍保持一次结构化检索在 FAQ 判断和后续融合链路中复用。

#### RAG API 测试隔离

文件：`backend/tests/test_rag.py`

- 增加仅在该测试文件生效的确定性生成 fixture；
- patch 的位置为消费端 `app.services.answer_orchestrator.generate_response`；
- fake 只读取真实 evidence context 并生成回答，不按问题返回预制景区答案；
- 强制断言 `context` 非空、`allow_mock_fallback=False`，确保没有放宽生产策略；
- 真实保留以下链路：
  - ASGI `/api/chat/text`；
  - QueryCoordinator；
  - 结构化、FTS、Chroma 检索；
  - 证据过滤和融合；
  - citation 构建；
  - 服务端首条引用绑定；
  - 交互记录和响应序列化。
- 六个内容用例同时要求 `sources` 和 `citations` 非空；
- 来源多样性改为比较两个主题的 `citations[].chunk_id` 集合；
- 性能测试使用 `retrieval.route` 区分 FAQ/RAG，不再从展示文本推断路由；
- 原有 10 秒 RAG 阈值未放宽。

#### RAG evaluator v2 契约

文件：`backend/tests/test_rag_evaluator.py`

- 将旧 `no_evidence_proxy` 测试改为 `rag-baseline-v2` 委托契约测试；
- 测试数据显式声明 `production_refusal`、生产套件 case ID、预期 route 和 citation validation；
- 使用 `StubService({})`，确保委托拒答样例不会错误调用 raw RAG；
- 验证 `production_refusal_cases.delegated_to == lingguide-demo-v1`；
- 验证拒答样例不计入 document recall；
- 验证旧 `no_evidence_proxy` 字段不存在。

生产评估器 `backend/tools/evaluate_rag.py` 未修改。

### 10.3 生产安全策略保持不变

本次未修改任何生产实现，尤其没有：

- 开启生产 `allow_mock_fallback`；
- 在 LLM 生成失败后保留候选 sources/citations；
- 降低证据置信度阈值；
- 放宽线上 LLM 超时或测试性能阈值；
- 禁用 Chroma、FTS 或结构化检索；
- 增加 FAQ 绕过 RAG 测试；
- 跳过失败测试。

LLM 失败仍会返回安全拒答并清空与正文不一致的候选证据。

### 10.4 分层验证结果

#### FAQ 意图测试

```text
14 passed in 0.03s
```

#### RAG evaluator 与 demo evaluator

```text
8 passed in 0.20s
```

#### RAG API 测试

```text
10 passed in 4.42s
```

其中最慢的单个检索测试为 1.88 秒，性能测试整体约 0.23 秒，不再依赖 DeepSeek 网络状态。

#### LLM 失败安全契约

覆盖 `test_answer_orchestrator.py`、`test_chat_failure_contract.py`、`test_llm_policy.py`、`test_llm_empty.py`：

```text
15 passed in 1.43s
```

#### 原 12 项所在测试文件合并复验

```text
30 passed in 3.68s
```

#### RAG 相关扩展回归

```text
42 passed, 182 deselected in 5.37s
```

#### 后端全量测试

```text
224 passed, 2 warnings in 37.22s
```

原 12 项失败已经全部关闭，后端全量测试恢复为 0 failed。

### 10.5 剩余 warning

目前有两个非阻塞 warning：

1. `backend/app/api/profile.py` 使用即将弃用的 `datetime.utcnow()`；
2. `backend/app/api/avatar.py` 存在重复 FastAPI Operation ID 提示。

它们不影响本轮天气、FAQ、RAG 或评估器功能，未在本次最小修复范围内处理。

### 10.6 最终状态

- 天气接口真实调用正常；
- 景区天气和默认对话语境已锁定无锡灵山胜境；
- 福建同名地点不会污染默认景区查询；
- RAG 默认测试不再依赖真实 DeepSeek；
- 生产 LLM 失败安全策略保持不变；
- 后端全量测试：`224 passed, 0 failed`。
