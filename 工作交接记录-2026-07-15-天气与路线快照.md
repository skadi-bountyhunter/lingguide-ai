# 灵境导游工作交接记录

## 日期

2026-07-15

## 本轮目标

处理两个运行时问题：

1. AI 对话询问天气时，景点“灵山”偶尔被高德解析到福建，导致天气回答失败或没有可见回复。
2. AI 对话页点击路线推荐后，路线页重新生成路线，导致景点集合和顺序与对话内容不一致。

未记录任何 API Key、App Secret、访问令牌或密码。

## 已完成修改

### 1. 天气地点归一化和福建结果拦截

文件：

- `backend/app/core/tools/amap_tools.py`

完成内容：

- 增加景区别名识别：
  - 灵山
  - 灵山景区
  - 灵山胜境
  - 灵山胜境景区
  - 灵山大佛
  - 灵山大佛景区
  - 无锡灵山
- 景区别名统一规范为“灵山胜境”，并强制向高德传递“无锡”城市约束。
- `_resolve_adcode()` 不再直接取第一条 geocode 结果。
- 对景区候选结果校验“江苏 + 无锡”，优先选择滨湖区和包含灵山的结果。
- 只有福建、福州、闽侯等非无锡候选时，返回失败，不继续请求天气。
- `amap_weather()` 和 `amap_weather_raw()` 均使用统一解析逻辑。
- 实况和预报均为空时，结构化天气接口返回 `ok=false`。
- 工具描述已明确本项目景区天气应使用灵山胜境，避免模型传入不明确的同名地点。

### 2. LLM 空回复兜底

文件：

- `backend/app/core/llm.py`

完成内容：

- LLM 返回 `None`、空字符串或纯空白时，不再发送空 chunk。
- 允许 Mock 降级时，改为调用 `_mock_reply()` 返回非空回复。
- `allow_mock_fallback=False` 时继续抛出 `LLMGenerationError`。
- 工具调用达到上限后的强制收尾路径也增加相同空内容处理。

### 3. WebSocket 错误可见化

文件：

- `backend/app/api/chat.py`
- `frontend-visitor/src/composables/useWebSocket.ts`

完成内容：

- WebSocket 后端异常记录内部日志，但向客户端发送固定的“服务暂时不可用，请稍后重试。”，避免泄露内部异常。
- `llm_done` 无先前 `llm_stream` 时，前端会创建新的 assistant 消息。
- `llm_done` 优先按当前流式消息 ID 更新，避免误更新上一条 assistant 消息。
- WebSocket `error` 事件会在游客端显示可见的失败提示，不再只输出到控制台。

### 4. 对话路线结构化快照

后端文件：

- `backend/app/api/chat.py`

前端文件：

- `frontend-visitor/src/stores/chat.ts`
- `frontend-visitor/src/composables/useWebSocket.ts`
- `frontend-visitor/src/views/ChatView.vue`
- `frontend-visitor/src/views/RouteView.vue`
- `frontend-visitor/src/types/route.ts`

完成内容：

- 增加明确路线意图判断。
- 只有路线规划、路线推荐、游览顺序、半天游/全天游等明确请求，才生成路线快照。
- 普通景点介绍、历史、特色、位置、时间问题不生成路线快照。
- 对话 WebSocket 中调用现有 `generate_route()` 生成一次结构化路线。
- `llm_done` 新增可选 `route_plan` 字段，包含：
  - `schema_version: 1`
  - `source: chat`
  - `title`
  - `duration`
  - `duration_mode`
  - 有序 `spots`
  - `tips`
  - `interests`
  - `sources`
- `route_plan.spots` 数组顺序为唯一游览顺序。
- `ChatMessage` 增加 `routePlan` 字段。
- 路线推荐按钮仅对有 `routePlan` 的 assistant 消息显示。
- 点击路线推荐时，使用 history state 传递当前消息自己的 `route_plan`，不再依赖 `chat_query/chat_reply` 二次生成。
- 路线页读取并校验快照后直接展示、上图和保存，不再自动请求 `/api/chat/route`。
- 快照非法或丢失时提示用户重新提出路线规划需求。
- `ScenicMap.vue` 原有地图初始化后补画路线逻辑未重复修改。

## 新增测试

- `backend/tests/test_amap_tools.py`
  - 福建候选在前、无锡候选在后时选择无锡。
  - 只有福建候选时 fail closed。
  - 实况/预报均为空时返回失败。
- `backend/tests/test_llm_empty.py`
  - 空内容允许 Mock 时返回非空。
  - 禁止 Mock 时抛出 `LLMGenerationError`。
- `backend/tests/test_chat_snapshot.py`
  - 明确路线意图判断。
  - 普通讲解不触发路线意图。
  - 路线快照保持景点顺序。

## 验证结果

后端定向测试：

```text
12 passed
```

后端全量测试：

```text
84 passed
```

游客端构建：

```text
npm run build 通过
```

构建仅有依赖注释警告和 chunk 体积警告，不影响构建成功。

## 服务状态

本轮测试结束后，已停止为验证临时启动的服务：

- 后端 8000：已停止
- 游客端 3000：已停止
- 管理端 3001：已停止
- Chrome 调试进程：未主动停止

## 重要设计说明

### 天气

天气修复只保证景区地点不会接受福建等非江苏无锡候选；如果真实高德服务未配置、网络失败或没有天气数据，会返回明确失败，而不是伪造天气。

### 路线

路线快照只在本轮 WebSocket 明确路线请求时生成。普通问答仍走原有 FAQ/RAG/LLM 流程，不附带路线快照。

路线页手动点击“AI 智能规划”仍然可以正常调用 `/api/chat/route`。只有从带路线快照的对话消息进入路线页时，才会跳过二次生成。

### 前端构建产物

游客端已重新构建，`frontend-visitor/dist` 已包含本轮路线快照和错误可见化改动。

## 后续建议

1. 启动真实服务后，用浏览器验证：
   - 对话询问“灵山天气”；
   - 对话提出“请规划一条先去梵宫，再去九龙灌浴的路线”；
   - 点击该消息的“路线推荐”；
   - 检查路线页不再发起第二次 `/api/chat/route`，卡片和地图顺序一致。
2. 检查当前运行环境读取的是 `backend/.env` 还是根目录 `.env`，避免 LLM/高德配置分叉。
3. 如天气仍失败，先查看后端日志中的工具调用地点和高德返回状态，不记录密钥。
4. 后续可补充真正的 WebSocket 端到端测试和前端单元测试，但本轮没有引入测试框架。
5. 当前 `generate_response()` 仍使用 OpenAI-compatible SDK 和现有 DeepSeek 配置，本轮没有迁移 LLM 提供商。
