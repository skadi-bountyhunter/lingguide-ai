# 灵境导游工作日志

## 日期

2026-07-15

## 今日完成

### 1. 路线规划真实步行路线升级

- 使用高德 `AMap.Walking` 返回的真实道路路径替代景点之间的简单直线。
- 修复多段路径嵌套传给单个 Polyline 的问题。
- 每个相邻景点独立绘制路线段。
- 成功段显示棕色真实道路曲线和方向箭头。
- 失败段单独显示绿色虚线，禁止跨失败段错误直连。
- 加入 `AMap.MoveAnimation`，实现单个箭头沿路线自动转向、循环移动。
- 支持 `prefers-reduced-motion`，减少动态效果时不启动移动箭头。
- 增加路线请求版本控制，避免快速切换路线时旧请求覆盖新路线。
- 完善路线覆盖物清理，离开页面时停止动画并移除路线覆盖物。

涉及文件：

- `frontend-visitor/src/components/ScenicMap.vue`

### 2. 数字人路线规划只识别一个景点问题修复

已确认根因：

- 路线接口复用了普通聊天的 mock 降级。
- LLM 超时后，路线提示词中的“通高88米”等内容触发了“灵山大佛”预设回复。
- 路线正则把普通景点介绍误识别成一个合法路线景点。
- JSON 和正则分支只要识别出一个景点就提前返回。
- 编号路线解析前被统一清洗，换行被压平，导致后续编号通常只识别第一项。

已完成修改：

- 新增 `LLMGenerationError`。
- 路线生成使用 `allow_mock_fallback=False`，不再调用普通聊天 mock。
- LLM 失败时改用真实景点候选生成结构化降级路线。
- JSON、编号文本、异常降级统一去重、排序和补足。
- 半天路线补足到 5 个景点；全天路线补足到 6 个，最多 8 个。
- 用户原话、LLM 结果、数字人回复中的景点按优先级合并。
- 数字人聊天点击“路线推荐”时，通过 history state 传递被点击 AI 回复。
- “重新规划”保留原始用户问题和 AI 回复语境。

涉及文件：

- `backend/app/core/llm.py`
- `backend/app/api/chat.py`
- `frontend-visitor/src/views/ChatView.vue`
- `frontend-visitor/src/views/RouteView.vue`
- `backend/tests/test_chat_route.py`

### 3. FAQ 误匹配问题修复

已确认根因：

- 原 FAQ 规则只要匹配到一个长度不少于 3 个字的关键词就直接返回。
- “九龙灌浴”本身被当成“表演时间 FAQ”的充分条件。
- 字符集合重叠规则会进一步扩大误匹配范围。
- FAQ 命中后直接跳过 RAG 和 LLM。

已完成修改：

- FAQ 改为“实体 + 意图”联合匹配。
- 九龙灌浴只有同时出现“几点、什么时候、表演时间、几场、场次”等时间意图词时，才命中表演时间 FAQ。
- 删除字符重叠直接命中逻辑。
- 增加路线意图保护：路线、规划、游览顺序、几个景点、一日游、半日游等问题不直接命中普通 FAQ。
- FAQ 管理模型增加 `entities`、`intent`、`intent_keywords`、`exact_questions` 字段。
- 离线 mock 改为先判断意图，再选择回复模板。
- 九龙灌浴的地点、寓意、介绍、路线等问题不再固定回答表演时间。

涉及文件：

- `backend/app/api/chat.py`
- `backend/app/faqs.json`
- `backend/app/api/knowledge.py`
- `backend/app/core/llm.py`
- `backend/tests/test_faq.py`
- `backend/tests/test_faq_intent.py`

## 验证结果

- 路线专项测试：5 passed。
- FAQ 专项测试：37 passed。
- 后端全量测试：68 passed。
- 游客端 `npm run build`：通过。
- 真实路线接口验证：原先只返回“灵山大佛”的请求现在返回 5 个真实、不重复景点。
- 数字人路线浏览器链路验证：请求同时携带 `chat_query` 和 `chat_reply`，路线卡片显示 5 个景点，地图可绘制多站路线。
- FAQ 真实接口验证：
  - “九龙灌浴几点表演”命中时间 FAQ。
  - “九龙灌浴在哪里”进入知识库问答。
  - “九龙灌浴有什么寓意”进入知识库问答。
  - “介绍一下九龙灌浴”进入知识库问答。
  - “规划一条经过九龙灌浴的路线”不会命中表演时间 FAQ。

## 今日服务状态

已按要求停止前后端服务：

- 后端 `8000`：已停止。
- 游客端 `3000`：已停止。
- 管理端 `3001`：已停止。
- Chrome 调试进程未因本次操作主动停止。

## 后续修改建议

### P0：完善 FAQ 意图体系

- 为所有 FAQ 补齐实体和意图字段，禁止新 FAQ 只填写宽泛关键词后直接上线。
- 管理端新增 FAQ 时增加实体、意图、意图关键词的必填校验。
- 增加 FAQ 冲突检测，例如同一实体同时存在时间、地点、历史等多个意图时，确保关键词不会交叉命中。
- 对低置信度问题交给 RAG/LLM，必要时反问用户具体想了解时间、地点还是特色。

### P1：统一三种对话入口

当前文本和 WebSocket 入口有 FAQ 预处理，语音入口路径不同。建议抽取统一问答服务：

```text
问题规范化 → 意图识别 → FAQ → RAG → LLM → 输出校验 → 情感/TTS
```

确保文本、WebSocket、语音和旧版数字人服务的回答规则一致。

### P1：加强景点实体和别名管理

- 建立景点别名表，区分“灵山梵宫/梵宫”“九龙浴佛/九龙灌浴”等合法别名。
- 避免用“佛”“灵山”等过短词直接匹配具体景点。
- 统一 FAQ、RAG、预设路线、数据库景点名称，减少知识库名称与地图景点名称不一致。

### P1：路线规划增加可观测性

建议路线响应或日志记录：

- 是否使用 LLM。
- 是否进入降级。
- 被解析出的景点数量。
- 被过滤的未知景点名称。
- 最终路线来源和处理耗时。

这样可以快速区分 LLM 输出问题、名称匹配问题和地图绘制问题。

### P2：优化地图路线体验

- 为路线规划增加明确的“正在规划/部分成功/降级路线”状态。
- 继续优化高德步行数据缺失时的分段提示。
- 后续可增加真实用户定位、偏航检测和重新规划；当前移动箭头只是路线动效，不等同于实时导航。
- 清理和校准预设路线中的无效景点名称，保证每条预设路线至少包含两个数据库可匹配景点。

### P2：增加浏览器回归场景

- 数字人对话点击路线推荐。
- 同一景点询问时间、地点、历史、寓意、路线等不同问题。
- LLM 超时时路线仍返回真实多景点。
- 快速切换路线、离开路线页、重新规划和减少动画偏好。

## 备注

本日志不记录 API Key、App Secret、访问令牌或密码。项目状态以后续代码、测试结果和实际服务状态为准。
