# 灵境导游 (LingGuide) 启动与停止说明

> 适用于 Windows 11，已按本机环境实测可用。
> 最后更新：2026-06-22

## 0. 端口约定

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API (FastAPI) | **8000** | 5000 被系统服务 MiguService 占用，**不可使用** |
| 游客前端 (Vite) | 3000 | |
| 管理后台 (Vite) | 3001 | |

> ⚠️ 绝不要用 5000 端口，也不要 taskkill 占用 5000 的进程，那是系统服务。

## 1. 前置依赖

打开 PowerShell 或 cmd，逐项确认：

```bat
where python
where node
where npm
where curl
python -m uvicorn --version
```

- `python / node / npm / curl` 都应返回路径
- 若 `python -m uvicorn --version` 报错，执行：`pip install uvicorn fastapi`

## 2. 启动步骤

需要开 **3 个终端窗口**，分别跑后端、游客端、管理端。

### 2.1 后端（端口 8000）

```bat
cd /d E:\ruanjianbei\backend
python -m uvicorn app.main:app --reload --port 8000 --host 0.0.0.0
```

看到 `Uvicorn running on http://0.0.0.0:8000` 即服务启动。
健康检查：浏览器访问 http://localhost:8000/api/health ，返回 `{"status":"healthy"}` 即存活。

首次演示或知识库版本变更后，在后端目录执行一次索引初始化：

```bat
cd /d E:\ruanjianbei\backend
python -m tools.init_demo --json
```

该命令只使用已有 ready canonical 文档创建 shadow 索引，依次完成 build、validate、activate，并在 readiness 未达到 `ready` 时以非零状态退出。FAQ JSON 同步不是默认步骤，如需显式同步使用 `--sync-faqs`。重复执行相同 canonical 版本会幂等返回，不会删除旧的 `hybrid-v1`/legacy FTS/Chroma 数据。

RAG 演示必须检查 http://localhost:8000/api/readiness：严格模式下 HTTP 200 且 `status=ready` 才算就绪；HTTP 503 或 `not_ready` 时不要开始答辩演示。该初始化命令仅适用于受控演示或已备份环境，不要现场覆盖文档或索引。

### 2.2 游客前端（端口 3000）

新开一个窗口：

```bat
cd /d E:\ruanjianbei\frontend-visitor
npm run dev
```

访问 http://localhost:3000

### 2.3 管理后台（端口 3001）

再开一个窗口：

```bat
cd /d E:\ruanjianbei\frontend-admin
npm run dev
```

访问 http://localhost:3001

## 3. 停止步骤

每个终端窗口按 `Ctrl + C` 即可停止对应服务。

若窗口已关闭但进程残留，按端口清理（**只清 8000/3000/3001，不要碰 5000**）：

```bat
for /f "tokens=5" %a in ('netstat -ano ^| findstr ":8000.*LISTENING"') do taskkill /PID %a /F
for /f "tokens=5" %a in ('netstat -ano ^| findstr ":3000.*LISTENING"') do taskkill /PID %a /F
for /f "tokens=5" %a in ('netstat -ano ^| findstr ":3001.*LISTENING"') do taskkill /PID %a /F
```

## 4. 常见问题

- **后端启动报 `uvicorn not found`**：用 `python -m uvicorn` 而非裸 `uvicorn`，或 `pip install uvicorn`。
- **端口被占**：用上面第 3 节的命令按端口清理，切勿用 5000。
- **前端代理 404**：确认后端已先于前端启动，且在 8000 端口。
- **图片缩略图不显示**：后端已挂载 `frontend-visitor/public/images` 到 `/images`，确认后端进程是最新启动的。
