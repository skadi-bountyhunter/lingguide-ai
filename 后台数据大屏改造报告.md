# 后台数据大屏改造报告

## 改造目标

将原有包含固定值、比例估算和错误统计口径的数据大屏，改造为基于已落库 `Interaction` 记录的真实运营交互看板，并提升管理端视觉层级、可读性和数据状态反馈。

## 已完成改造

### 真实数据统计

已重构 `backend/app/services/dashboard_service.py`：

- **交互次数**：统计范围内真实 `Interaction` 记录数。
- **服务会话数**：按 `session_id` 去重统计，不再误称为游客人数。
- **平均处理耗时**：只计算 `thinking_time_ms > 0` 的有效样本，并返回样本数。
- **平均输入情绪得分**：只计算 `0–1` 范围内的 `emotion_score`，明确标注为输入情绪分析，不再称为满意度。
- **最近活动**：统计最近 15 分钟内的活跃会话数及交互次数。
- **交互方式构成**：按真实 `query_mode` 聚合为文本、语音和其他方式。
- **交互趋势**：今日按小时、近 7 天和近 30 天按中国自然日统计。
- **情绪趋势**：按时间桶计算有效情绪得分均值；无有效样本返回 `null`，不会伪装为零分。
- **重复问句 TOP 8**：在当前周期内对完整原始问句进行真实频次统计，按次数降序、问句文本升序排序。
- **中国时区口径**：统一使用 `Asia/Shanghai`，修复 UTC 导致的跨日和时段错位。
- **空数据语义**：计数为 `0`、均值为 `null`、排行榜为空数组，不返回默认演示数据。
- **故障语义**：移除宽泛异常后伪造全零数据的逻辑，避免把接口故障误显示为“暂无数据”。

### 接口升级

已修改 `backend/app/api/dashboard.py`：

- 保持接口路径：`GET /api/dashboard/overview`。
- 支持周期参数：`period=today|7d|30d`。
- 非法周期由 FastAPI 返回 `422`。
- 响应设置 `Cache-Control: no-store`，避免读取缓存统计。
- 复用现有 `require_admin` 权限依赖；开发模式保留项目原有的无令牌兼容行为。

### 管理端视觉与交互

已重构 `frontend-admin/src/views/DashboardView.vue`：

- 新增深松绿运营总览区、当前统计周期、数据生成时间和手动刷新入口。
- 新增“今日 / 近 7 天 / 近 30 天”统一周期切换，所有模块使用同一周期数据。
- 替换为四个真实 KPI：交互次数、服务会话、平均处理耗时、平均输入情绪得分。
- 使用 ECharts 实现交互趋势单序列柱状图和平均输入情绪得分单序列折线面积图。
- 图表支持 tooltip、无障碍描述和可展开的数据明细表。
- 下方模块使用真实数据展示重复问句 TOP 8、最近 15 分钟活动和交互方式构成。
- 新增骨架加载、真实空状态、请求失败提示和保留上一次成功数据的刷新策略。
- 每 60 秒自动刷新，同时支持手动刷新。
- 增加响应式布局，避免较窄管理端内容区出现图表或长问句溢出。
- 分类色固定为绿、黄、蓝顺序，并通过色度、色觉缺陷分离度和正常视觉分离度校验。
- 图表标记统一为 4px 数据端圆角、2px 折线、10% 面积填充和实线弱化网格。

### 浏览器核验脚本

已升级 `scripts/inspect-dashboard.js`：

- 自动点击“今日 / 近 7 天 / 近 30 天”。
- 等待每个周期加载结束后采集 KPI、图表、问句和方式构成。
- 记录 `/api/dashboard/overview` 请求状态。
- 单独输出所有 `4xx/5xx` 资源和浏览器控制台错误。
- 生成最终桌面截图 `uploads/_debug/admin-dashboard-audit.png`。

## 已移除的伪数据展示

以下展示已删除，不再对外产生误导：

- “当前在线游客 = 今日交互数”；
- 按比例估算的“AI 对话进行中”；
- 按比例估算的“路线规划中”；
- 恒定为零的“数字人服务中”；
- 固定的咨询类别百分比；
- 前端关键词猜测的咨询分类；
- 空数据时的假柱状图、默认问题和伪满意度；
- 静默吞没接口错误后显示零值的逻辑。

## 测试覆盖

`backend/tests/test_dashboard.py` 覆盖：

- 中国时区边界统计；
- 同会话多轮交互去重；
- 交互数、趋势桶和方式构成的统计一致性；
- 无效耗时与无效情绪样本过滤；
- 空数据时的 `0`、`null` 与空列表语义；
- 三种合法周期与非法周期参数验证。

## 最终验证结果

验证日期：2026-07-19。

### 后端测试

执行：

```bash
cd E:/ruanjianbei/backend
python -m pytest tests/test_dashboard.py -q
```

结果：`3 passed in 0.32s`。

存在一条非阻塞的 `pytest-asyncio` 配置弃用提示：项目尚未显式设置 `asyncio_default_fixture_loop_scope`，不影响本次测试结果。

### 管理端构建

执行：

```bash
npm --prefix E:/ruanjianbei/frontend-admin run build
```

结果：

- `vue-tsc` 类型检查通过；
- Vite 生产构建通过；
- IDE 对 `DashboardView.vue` 的静态诊断为零错误。

构建仅出现依赖包 Rollup 注释警告和 ECharts 相关 chunk 超过 500 kB 的体积提示，均不是本次功能错误。

### 运行时接口

后端运行于 `http://localhost:8000`，实测结果如下：

| 周期 | HTTP | Cache-Control | 交互总数 | 趋势桶合计 | 方式构成合计 | 一致性 |
|---|---:|---|---:|---:|---:|---|
| `today` | 200 | `no-store` | 34 | 34 | 34 | 通过 |
| `7d` | 200 | `no-store` | 878 | 878 | 878 | 通过 |
| `30d` | 200 | `no-store` | 986 | 986 | 986 | 通过 |
| `invalid` | 422 | — | — | — | — | 通过 |

近 30 天情绪统计另行核对：汇总有效样本数为 `986`，趋势桶有效样本合计为 `986`，按桶样本数加权后的均值为 `0.7365`，与汇总值完全一致。

以上数字是验证时数据库快照，后续新增交互后会变化。

### 浏览器端到端

管理端运行于 `http://localhost:3001/dashboard`，通过 Chrome CDP 实测：

- 三个周期按钮均可点击，选中状态正确；
- 三个 `/api/dashboard/overview` 请求均返回 `200 application/json`；
- 三个周期均渲染 4 个 KPI、2 个 ECharts 图表、TOP 问句和 3 种交互方式；
- 页面未出现刷新错误或空白区域；
- 未发现 `4xx/5xx` 资源；
- 浏览器控制台无错误；
- 1024px 与 760px 窄视口均无页面级横向溢出；
- 1024px 下明细表可正常展开，KPI 自动切换为两列，图表切换为单列；
- 页面未恢复“在线游客”“路线规划中”“数字人服务中”等伪数据文案；
- “非满意度”是情绪指标的必要口径说明，不是满意度统计回归。

截图：

- `uploads/_debug/admin-dashboard-audit.png`
- `uploads/_debug/admin-dashboard-1024.png`

### 色板验证

使用 `dataviz` 工具附带的 `validate_palette.js` 对 `#008300,#EDA100,#2A78D6` 执行浅色、白色表面校验。

结果：色度、色觉缺陷相邻分离度、正常视觉分离度均通过。黄色在白色背景上的对比度低于 `3:1`，但界面始终同时显示方式名称、数量、百分比和独立色点，并提供数据文本，因此颜色不是唯一信息通道。

## 关键接口契约

```text
GET /api/dashboard/overview?period=today|7d|30d

period / timezone / generated_at / range
summary.interaction_count
summary.session_count
summary.avg_thinking_time_ms.{value,sample_count}
summary.avg_emotion_score.{value,sample_count}
activity.{window_minutes,active_session_count,interaction_count}
mode_distribution.{total,items[{mode,count,ratio}]}
interaction_trend.{granularity,buckets[{start,end,label,count}]}
emotion_trend.{granularity,buckets[{start,end,label,avg_score,sample_count}]}
top_questions[{question,count}]
```

约束：

- 所有时间输出均为中国时区 ISO 时间。
- `avg_*.value`、趋势桶的 `avg_score`、无分母时 `ratio` 可为 `null`，表示没有有效样本，不应改回 `0`。
- `session_id` 只是服务会话标识，不可称为实名游客或独立用户。
- `avg_emotion_score` 是用户输入的自动情绪分析，不等于游客满意度。
- 不得恢复“在线游客”“路线规划中”“数字人服务中”等没有持久化观测依据的展示。

## 验收结论

后台数据大屏的真实数据统计、接口契约、管理端交互、响应式布局、图表可访问性、自动化浏览器核验和运行时验证均已完成。本轮计划已闭环，无阻塞项。

---

## 增量功能（2026-07-31）

### 新增 analytics3d 区四个组件

初版改造完成后，在 `analytics3d-grid` 区新增四个分析模块，均消费独立后端接口，无固定值或估算值。

| 组件文件 | 标题 | 接口 | 数据源 |
|---|---|---|---|
| `SatisfactionBall.vue` | 整体满意度 | `GET /api/analytics/satisfaction?days=N` | `Interaction.emotion_score` 均值转百分比 |
| `FeedbackWordCloud.vue` | 游客反馈词云 | `GET /api/analytics/word-freq?days=N` | `Interaction.query_text` jieba 分词频率 |
| `PinsHeatmap.vue` | 游客标记热力图 | `GET /api/visits/heatmap?days=N` | `UserVisit.lng/lat`，高德 JS API + HeatMap 插件渲染，中心坐标 `[120.100, 31.423]` |
| `EmotionBar3D.vue` | 情绪时空强度矩阵 | `GET /api/analytics/emotion-3d?days=N` | `Interaction JOIN Spot`，按小时 × 景点分组，echarts-gl bar3D 渲染 |

后端实现位于：
- `backend/app/services/analytics_service.py` — `get_satisfaction`、`get_word_freq`、`get_emotion_3d`
- `backend/app/api/visits.py` — `GET /api/visits/heatmap`
- `backend/app/api/analytics.py` — 其余三个接口路由

`UserVisit` 模型（`backend/app/models/__init__.py`）：`id / spot_id / lng / lat / created_at`，游客点击景点标记时静默写入，用于热力图密度计算。

### 布局调整

- 热力图移至情绪矩阵前（先展示地理分布，再展示时域分析）。
- 热力图容器高度：480 px（`PinsHeatmap.vue` scoped CSS）。
- 情绪矩阵 min-height：320 px（`EmotionBar3D.vue` scoped CSS，降低以突出地图的视觉权重）。
- 情绪三维图原名"景点情绪热力图"，重命名为**情绪时空强度矩阵**，kicker 改为 `EMOTION · SPACETIME MATRIX`。

### 测试数据脚本

`scripts/seed_dashboard_data.py`：从 `spots` 表读取真实 GCJ-02 坐标，在各景点 50 m 范围内随机生成 200 条 `user_visits` 和 150 条 `interactions`（含 `spot_id`、`emotion_score`、小时分布），用于在无真实游客数据时验证热力图和情绪矩阵的渲染链路。
