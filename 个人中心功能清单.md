# 个人中心功能清单

> 当前状态以代码、专项测试、前端构建和浏览器联调结果为准。

## 一、已打通功能

| 功能 | 实现情况 |
|---|---|
| 进入个人中心 | 底部导航可进入“我的”，子页面路由完整 |
| 注册、登录、退出 | SQLite 持久化账号；登录态本地保存；退出后清理账号和对话会话 |
| 忘记密码 | 验证码校验、更新密码、验证码一次性失效已实现 |
| 真实用户资料 | `GET/PATCH /api/profile/me`；展示真实昵称、脱敏手机号并支持编辑昵称 |
| 真实个人统计 | 游览次数、景点收藏、保存路线、对话次数均按当前用户实时统计 |
| 保存路线 | 前后端、数据库、增删查及用户隔离已实现 |
| 景点收藏 | 添加、检查、列表、详情页取消、列表删除和类型跳转已实现 |
| 收藏鉴权 | 统一使用真实登录用户 ID；未登录返回 401；用户数据隔离 |
| 游览记录 | 景点详情自动记录；同一景点累计次数；列表按最近访问排序 |
| 语音设置 | 音色、语速、音量使用版本化 `localStorage` 持久化，并作用于普通 TTS |
| 意见反馈 | 游客提交与历史记录、管理端分页筛选、回复及状态更新已实现 |
| 消息通知 | 全体/个人通知、列表、未读统计、独立已读状态、管理端发布已实现 |
| 实时通知 | WebSocket 登录鉴权、在线推送、断线重连和 HTTP 补拉已实现 |
| 对话归属 | 文本、语音和 WebSocket 新对话均可绑定登录账号；历史匿名数据不误归属 |
| 四语切换 | 游客端静态 UI 支持中、英、日、韩四语，选择可持久化 |
| 门票价格 | 参考价格页及信息时效声明已实现 |
| 素斋介绍 | 简介、地点信息和预约状态说明已实现 |
| 隐私政策 | 静态页面已实现 |
| 关于页面 | 静态页面已实现 |
| 管理端反馈/通知 | 已增加侧栏入口、反馈处理页和通知发布页 |

## 二、功能边界

- 四语覆盖游客端生产页面的静态界面；景点资料、知识库内容、路线结果和 AI 动态回答保持数据源原文。
- 语音设置作用于普通 TTS；魔珐星云 3D 数字人音色仍由数字人角色配置控制。
- 门票和素斋当前提供参考信息，不包含支付或真实预约。
- 停车导航、紧急求助和离线地图暂未接入第三方服务，入口会明确提示“开发中”，不会静默或制造假成功。
- 当前通知 WebSocket 连接管理器适用于单后端实例；多实例部署时需改用 Redis 等跨进程消息总线。
- 历史 `anonymous/dev/test` 收藏不自动归属真实用户，也不删除。

## 三、本轮验证结果

### 后端

- 个人中心专项测试：`4 passed`。
- 核心回归测试：`25 passed`，覆盖保存路线、聊天路线、语音路线、聊天 WebSocket、数字人预设。
- 后端完整测试：`197 passed, 4 failed`。4 个失败均为既有 RAG 测试/实现契约漂移：
  - `test_faq_intent.py` 3 个测试仍以旧参数调用 `_faq_covers_named_spots()`。
  - `test_rag_evaluator.py` 1 个测试仍期待已不存在的 `no_evidence_proxy` 字段。
  - 个人中心新增专项测试全部通过。

### 前端

- 游客端 `vue-tsc + vite build` 通过。
- 管理端 `vue-tsc + vite build` 通过。
- 仅存在既有的大 chunk 与第三方 PURE 注释警告，无编译错误。
- VS Code 对本轮关键 Vue、TypeScript、Python 文件未报告诊断错误。

### 浏览器联调

已在实际运行的 `8000/3000/3001` 服务上验证：

- 真实昵称、脱敏手机号和 4 项真实统计正常渲染。
- 日语切换后个人中心、语音、反馈和通知页面即时生效。
- 语音音色/语速/音量会写入 `lingguide_voice_settings_v1`。
- 游客通知 WebSocket 显示已连接。
- 管理端反馈与通知页面、表格和空状态正常渲染。

截图位于 `uploads/_debug/`：

- `profile-features.png`
- `profile-language-ja.png`
- `profile-voice.png`
- `profile-feedback.png`
- `profile-notifications.png`
- `admin-feedback.png`
- `admin-notifications.png`

## 四、后续独立任务

以下不属于本轮个人中心闭环，建议单独排期：

1. 将兼容 token/明文密码升级为正式密码哈希、签名令牌、过期和服务端注销机制。
2. 修复后端现有 4 个 RAG 测试/实现契约漂移。
3. 多实例部署时将通知广播改为跨进程消息总线。
4. 接入真实支付、预约、停车导航、紧急位置上报和离线地图下载。
