# 灵境导游

这是一个具备多模态交互能力（语音、文本、表情）的 AI 数字人导游软件，为游客提供实时智能问答、个性化路线讲解和情感互动，并为景区管理方提供游客反馈分析。

## 项目约束

- 后端服务端口统一使用 `8000`。
- 游客端使用 `3000`，管理端使用 `3001`。
- 项目状态以当前代码和测试结果为准；auto-memory 仅作为历史线索，引用前必须核验。
- 禁止把 API Key、App Secret、访问令牌或密码写入源码、CLAUDE.md、memory 和日志。
