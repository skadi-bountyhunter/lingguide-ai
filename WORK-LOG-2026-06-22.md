# 工作日志 — 2026-06-22

## 概览

今日主任务：**推荐路线页地图模块改造**（阶段六）。将 `ScenicMap.vue` 从硬编码 8 个丑 label 标记，改为从后端 `/api/spots` 取数、自定义水滴形 marker、点击弹 InfoWindow 跳转详情的完整链路，并通过 CDP 端到端验证。

```
阶段六 ████████████ 地图模块改造  100% ✅（今日完成）
```

---

## 一、后端：Spot 模型加坐标字段

### 1.1 模型层 `backend/app/models/spot.py`
- 新增两列：`lng = Column(Float, nullable=True)` / `lat = Column(Float, nullable=True)`
- 经度/纬度（高德 GCJ-02 坐标系），允许为空（表示无定位）

### 1.2 数据库迁移 `backend/app/core/database.py`
- SQLite 的 `create_all` 不会自动给已有表加列，需手动 ALTER
- `init_db()` 末尾追加幂等迁移：`PRAGMA table_info(spots)` 检查列是否存在，没有则 `ALTER TABLE spots ADD COLUMN lng REAL` / `lat REAL`
- 迁移后检测 `lng IS NULL` 的记录，按 name 匹配补坐标

### 1.3 API 层 `backend/app/api/spots.py`
- `SpotCreate` / `SpotUpdate` / `SpotOut` 三个 Pydantic schema 均加 `lng: Optional[float] = None` / `lat: Optional[float] = None`
- `_apply()` 处理坐标字段（独立处理，None 表示清除定位）
- `_to_out()` 输出 `lng=s.lng, lat=s.lat`
- `SEED_SPOTS` 6 个景点填真实灵山胜境坐标：

| 景点 | 经度 | 纬度 |
|------|------|------|
| 灵山大佛 | 120.1032 | 31.4247 |
| 梵宫 | 120.0995 | 31.4228 |
| 九龙灌浴 | 120.1003 | 31.4235 |
| 五印坛城 | 120.0972 | 31.4215 |
| 降魔浮雕 | 120.1038 | 31.4252 |
| 菩提大道 | 120.0985 | 31.4220 |

---

## 二、前端数据层 `frontend-visitor/src/data/spots.ts`

- `ScenicSpot` interface 加 `lng?: number` / `lat?: number`
- `mapSpot(raw)` 转换加 `lng: raw.lng, lat: raw.lat`（snake_case → camelCase）

---

## 三、前端地图组件重写 `frontend-visitor/src/components/ScenicMap.vue`

### 3.1 取数
- 删除硬编码的 `scenicSpots` 数组
- `onMounted` 调 `fetchSpots()` 取数，过滤掉无坐标景点
- 并行加载高德 SDK 和景点数据：`Promise.all([load(), fetchSpots()])`

### 3.2 自定义 marker
- 用 `AMap.Marker` 的 `content` 属性传自定义 HTML（高德 2.0 支持）
- `markerHtml(spot, idx)` 生成水滴形 pin：圆形 emoji badge + 序号角标
- 全局 CSS `.map-pin-wrap` / `.map-pin-badge` / `.map-pin-num`（scoped 不作用到高德注入 DOM，必须全局样式）
- `map.setFitView(markers)` 默认聚焦所有标记

### 3.3 InfoWindow
- 预创建共享 `AMap.InfoWindow`，`closeWhenClickMap: true`
- `openInfo(spot)` 用自定义 HTML 填充：
  - 景点缩略图（`spot.image`，经 vite 代理到后端）
  - 景点名 + tags
  - 短描述（`spot.desc`，3 行省略）
  - 「查看详情 →」按钮（`data-name` 属性）
- 事件委托 `onInfoClick(e)` 监听 `[data-name]` 点击，调 `goDetail(name)` 跳转 `/spot/{name}`
- 全局 CSS `.iw-card` / `.iw-thumb` / `.iw-body` / `.iw-desc` / `.iw-btn`，并覆盖高德默认 `.amap-info-content` 样式

### 3.4 工具栏
- 保留三个按钮：定位（`setZoomAndCenter`）/ 路线（`AMap.Polyline` 连接所有景点，森林绿虚线）/ 全景（`setFitView`）

---

## 四、端到端验证

### 4.1 验证脚本 `scripts/inspect-route-map.py`
- 因高德 marker click 事件不能用 DOM `marker.click()` 触发，改用 CDP `Input.dispatchMouseEvent` 模拟真实鼠标事件（mouseMoved → mousePressed → mouseReleased）
- 连接 localhost:9222 CDP，导航到 http://localhost:3000/route，检查 marker 渲染 → 截图 → 点击第一个 marker → 检查 InfoWindow → 点「查看详情」→ 验证跳转

### 4.2 验证结果（全通过）

```
DOM检查: markers=6, containerExists=true, firstMarkerRect cx=493.46, cy=87.95
InfoWindow: hasCard=true, title="灵山大佛", descLen=39, hasBtn=true, btnName="灵山大佛", hasThumb=true
点击详情后URL: http://localhost:3000/spot/灵山大佛

  ✅ 地图容器渲染
  ✅ 景点标记数量 = 6
  ✅ 点击标记弹出 InfoWindow
  ✅ InfoWindow 有标题
  ✅ InfoWindow 有描述
  ✅ InfoWindow 有缩略图
  ✅ InfoWindow 有详情按钮
  ✅ 点击详情跳转 /spot/
=== 8/8 通过 ===
```

截图保存于 `uploads/_debug/route-map-1/2/3.png`。

---

## 五、改动文件清单

| 文件 | 改动 |
|------|------|
| `backend/app/models/spot.py` | 加 lng/lat 列 |
| `backend/app/core/database.py` | 加幂等迁移逻辑 |
| `backend/app/api/spots.py` | schema 加字段 + 种子坐标 |
| `frontend-visitor/src/data/spots.ts` | interface + mapSpot 加坐标 |
| `frontend-visitor/src/components/ScenicMap.vue` | 整体重写 |
| `scripts/inspect-route-map.py` | 新增 CDP 验证脚本 |

---

## 六、风险点与解决

1. **SQLite 加列迁移**：`ALTER TABLE ADD COLUMN` 可行但需幂等检查，用 `PRAGMA table_info` 避免重复加列报错
2. **高德 InfoWindow 样式**：scoped CSS 不作用到高德注入 DOM，改用全局 `<style>` 块 + inline style
3. **marker click 无法 DOM 触发**：改用 CDP `Input.dispatchMouseEvent` 模拟真实鼠标事件
4. **端口冲突**：后端固定 8000（5000 被咪咕 MiguService 系统服务占用）

---

## 七、运行状态

- 后端: localhost:**8000**
- 游客端: localhost:3000（路线页 `/route` 地图模块已可用）
- 管理端: localhost:3001
- 两端 vite proxy 均指向 :8000
